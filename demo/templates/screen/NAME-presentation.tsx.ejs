import React, { memo } from "react"
import { View, Text } from "react-native"
import { Screen } from "../../components/screen/screen";
import { <%= props.pascalCaseName %>Props } from "./<%= props.kebabCaseName %>-interface"

export const <%= props.pascalCaseName %>Presentation = memo(({
  onPressBack
}:<%= props.pascalCaseName %>Props): JSX.Element => {
  return (
    <Screen
      onPressBack={onPressBack}
      title={"<%= props.pascalCaseName %> Screen"}
    >
      <View>
        <Text>
          {"More Content Coming Soon!"}
        </Text>
      </View>
    </Screen>
  )
})
