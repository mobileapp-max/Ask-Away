export const use<%= props.pascalCaseName %>Functions = (props: any) => {

  const { navigation, route } = props
  const { params } = route
  const { incomingVariable = {} } = params

    const onPressBack = (): void => {
      navigation.goBack()
    }

    return {
      onPressBack
    }
}
